services:
  tv-live:
    build: .
    container_name: tv-live
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config
      - ./playlist:/playlist
    environment:
      - TZ=Asia/Shanghai
